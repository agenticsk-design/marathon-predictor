services:
  - type: web
    name: marathon-predictor-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python model/train.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
